language: java

jdk:
  - oraclejdk8

sudo: required
install: true
dist: trusty
script:
  - sudo apt-get update && sudo apt-get install oracle-java8-installer
  - java -version
  
branches:
  except:
  - /^(?i:dev)-.*$/